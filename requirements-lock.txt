# shadowcal — pinned, KNOWN-GOOD versions (generated from a working macOS venv).
#
# Use this for reproducible installs (especially when sharing with colleagues):
#     pip install -r requirements-lock.txt
#
# requirements.txt keeps looser minimums for flexibility; this file pins the exact
# versions shadowcal was tested against. Regenerate after upgrading + re-testing:
#     pip freeze > requirements-lock.txt   (then trim to the entries below)

# --- core (required for headless 'sync') ---
icalendar==7.1.3
recurring-ical-events==3.8.2
pyobjc-core==12.2
pyobjc-framework-Cocoa==12.2
pyobjc-framework-EventKit==12.2

# --- TUI ('shadowcal' / 'shadowcal tui') ---
textual==8.2.7
rich==15.0.0
markdown-it-py==4.2.0
mdit-py-plugins==0.6.1
linkify-it-py==2.1.0
uc-micro-py==2.0.0
Pygments==2.20.0
platformdirs==4.10.0
typing_extensions==4.15.0

# --- menu-bar app ('shadowcal menubar') ---
rumps==0.4.0

# --- transitive (icalendar / recurring-ical-events) ---
python-dateutil==2.9.0.post0
six==1.17.0
tzdata==2026.2
x-wr-timezone==2.0.1
click==8.4.1
